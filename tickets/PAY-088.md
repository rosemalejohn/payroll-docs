# [GW] Payroll - Comprehensive Pest Test Suite Setup

## Description

Set up the foundational Pest testing framework with helpers, factories, and comprehensive test suites covering authentication, authorization, payroll computation, and the full payroll run lifecycle. This ensures the application has reliable automated testing from the start.

## Technical Implementation

### Test Configuration
- Configure Pest in `tests/Pest.php` with:
  - `uses(TestCase::class, RefreshDatabase::class)` for feature tests
  - Parallel testing support via `--parallel` flag
  - Custom expectations if needed

### Test Helpers — `tests/Helpers/`
- **`PayrollTestHelpers.php`** (trait or helper functions):
  - `actingAsRole(string $role)`: creates user with given role, authenticates, returns user
  - `seedSystemData()`: seeds company, roles, PHP currency, 11 salary components, 4 bonus types
  - `createPayrollRun(array $overrides = [])`: creates a full payroll run with period, employees, compensation, and computed payslips
  - `createEmployeeWithCompensation(array $overrides = [])`: creates employee + compensation record
  - `assertPayslipAmounts(Payslip $payslip, array $expected)`: custom assertion for payslip verification

### Test Suites

**Authentication & Authorization — `tests/Feature/Auth/`**
- `LoginTest.php`: valid login, invalid credentials, inactive user blocked
- `RoleAccessTest.php`: each role can access their allowed routes, blocked from restricted routes
  - Owner: all routes accessible
  - Payroll officer: cannot access finalize endpoint (403)
  - Finance: cannot access employee CRUD (403)
  - Employee: can only access own payslips and dashboard (403 on everything else)

**Employee — `tests/Feature/Employee/`**
- `EmployeeCrudTest.php`: create, update, list, filter, deactivate
- `EmployeeCompensationTest.php`: update salary, change currency, validation errors
- `EmployeePayrollFlagsTest.php`: owner can toggle external salary, payroll_officer cannot

**Payroll Run Lifecycle — `tests/Feature/PayrollRun/`**
- `CreatePayrollRunTest.php`: creates run, auto-populates employees, snapshots currencies, prevents duplicates
- `PayrollRunStateTest.php`: draft→submitted→under_review→finalized transitions, invalid transitions blocked
- `SubmitPayrollRunTest.php`: validates before submit, notifies finance
- `FinalizePayrollRunTest.php`: only owner can finalize, locks payslips, locks period

**Payroll Computation — `tests/Feature/Computation/`**
- `GrossPayTest.php`: monthly salary correct, daily rate × working days, foreign currency conversion
- `SssDeductionTest.php`: bracket lookup returns correct employee share for various salary levels
- `PhilhealthDeductionTest.php`: 2.5% with floor (₱10,000) and ceiling (₱100,000)
- `PagibigDeductionTest.php`: always ₱100
- `BirTaxTest.php`: each TRAIN Law bracket verified (≤250k=0, 250k-400k, 400k-800k, etc.)
- `NetPayTest.php`: gross + bonuses - deductions, floor at zero
- `ExternalSalaryTest.php`: steps 3-9 skipped, net=gross+bonuses, zero deductions

**Currency — `tests/Feature/Currency/`**
- `CurrencyManagementTest.php`: create, update rate, deactivate, base currency immutable
- `CurrencySnapshotTest.php`: snapshot created on run creation, snapshot rate used in computation

**Bonus — `tests/Feature/Bonus/`**
- `AutoTriggerBonusTest.php`: 13th month in December, June special in June, meal allowance every month
- `BonusExclusionTest.php`: excluded employee doesn't receive bonus
- `BonusComputationTest.php`: fixed, percentage of gross, percentage of basic, formula

**Cash Advance — `tests/Feature/CashAdvance/`**
- `CashAdvanceCrudTest.php`: create, list, balance calculation
- `CashAdvanceRecoveryTest.php`: balance updated after finalization, FIFO order

**Access Control — `tests/Feature/AccessControl/`**
- `PayrollOfficerRestrictionTest.php`: cannot finalize, finalize API returns 403
- `EmployeePayslipAccessTest.php`: cannot see draft payslips, direct URL returns 403, only sees own finalized
- `OwnerOnlyActionsTest.php`: external salary toggle, role assignment, finalization

### Factories
- Ensure all model factories are created with realistic data:
  - `CompanyFactory`, `UserFactory`, `EmployeeFactory`, `EmployeeCompensationFactory`
  - `CurrencyFactory`, `PayrollPeriodFactory`, `PayrollRunFactory`, `PayslipFactory`
  - `BonusTypeFactory`, `CashAdvanceFactory`

### Tests
- Run: `php artisan test` (all suites)
- Run specific: `php artisan test --filter=BirTaxTest`
- Run parallel: `php artisan test --parallel`

## Acceptance Criteria

- [ ] `php artisan test` runs all test suites without errors
- [ ] Test helpers reduce boilerplate (seedSystemData, actingAsRole, createPayrollRun)
- [ ] Authentication tests verify login/logout and role-based route protection
- [ ] Computation tests verify correct PHP amounts for all 10 steps
- [ ] SSS bracket lookup returns correct values for boundary cases
- [ ] BIR tax brackets verified for each tier including ≤250k (zero tax)
- [ ] External salary path verified: zero deductions, net = gross + bonuses
- [ ] Currency conversion tests verify snapshot rate usage
- [ ] Bonus auto-trigger tests verify month-based logic (December, June, all months)
- [ ] Access control tests verify payroll_officer cannot finalize (403)
- [ ] Access control tests verify employee cannot see draft payslips (403)
- [ ] All model factories generate valid, seedable data
- [ ] Tests are CI-ready (no external dependencies, deterministic)
